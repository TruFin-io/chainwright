---
title: Running in CI
description: Use Chainwright in headless CI environments.
sidebar:
  order: 4
---

Notes for running Chainwright inside a CI runner.

## Dependencies

Browsers, system libraries, and Playwright cache.

## Caching profiles in CI

Persisting the Chainwright cache across runs.

## Example workflow

```yaml
# .github/workflows/e2e.yml
```
